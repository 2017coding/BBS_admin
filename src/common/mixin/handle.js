export default {
  methods: {
    handleEvent (type, params) {
      switch (type) {
      case 'add':
        break
      case 'update':
        break
      case 'delete':
        break
      }
    }
  }
}
